# Ignore the bin and obj folders that are generated by dotnet build and dotnet publish
bin/
obj/

# Ignore the .vscode folder that contains the settings and configuration files for VS Code
.vscode/
.env

# Ignore the launchSettings.json file that contains the launch settings for debugging
Properties/launchSettings.json

# Ignore any user secrets that are stored in a JSON file
secrets.json


